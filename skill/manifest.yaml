name: WCP SPA Theme Settings
id: com.penrithbeacon.wcp-spa-theme-settings
uuid: 515af161-bbcc-4170-a376-7b8f74480849
version: 2.0.3
description: Embed WCP theme switching (Settings modal, .wcpt import, URL sharing, seasonal collections) into any WCP-compliant SPA
synopsis: |
  Adds a complete, ready-to-use theme settings system to an existing
  single-page application that already uses the WCP (Widget Context
  Protocol) `--wcp-*` CSS custom property token layer -- a gear-icon
  settings button, a tabbed modal (Theme / About / WCP), a theme engine
  that reads and applies `--wcp-*` variables, `.wcpt` file import via
  JSZip, shareable theme URLs, and an optional seasonal collections
  dropdown with four bundled collections (Spring, Summer, Autumn, Winter).
  Rather than building a settings UI from scratch, the agent copies a set
  of proven CSS/HTML/JS fragments into the target site in a defined order.

  Reach for this whenever a WCP-compliant SPA needs its users to be able
  to switch themes, share a themed URL with someone else, or import a
  custom `.wcpt` theme file, without writing that system by hand. The
  target SPA must already use `--wcp-*` CSS custom properties as its
  design token layer -- see WCP SPA WCP Compliance for that prerequisite
  step if it doesn't yet.

  Ships with Python contract tests verifying the token and id
  cross-references between the copied fragments stay consistent, and
  carries the same SYSTEM.md external-registry verification as every
  other package in this ecosystem.
author: Anthony Harrison
authorEmail: anthony@harrisonofthenorth.com
entry: SKILL.md
system_protocol_version: "1.0.0"
license: MIT
minimum_runtime: "1.0.0"
wcpVersion: "2.2.1"
capabilities:
  - filesystem.read
  - filesystem.write
homepage: https://openaiskillpackage.com/
repository: https://github.com/PenrithBeacon/AISKILL-WCP-SPA-THEME-SETTINGS
tags:
  - wcp
  - theme
  - spa
  - settings-modal
  - collections
type: procedural
changelog:
  - version: 2.0.3
    changes:
      - "BREAKING (.aiskill spec v2.3.0): new required manifest field synopsis — a multi-paragraph, hand-authored expansion of description, feeding both README.md's opening and CARD.md's rendering"
      - "Unified README.md: repo-root and skill/README.md are now byte-identical (previously diverged), and both fixed a stale v1.3.2 version reference left over since the 2.0.x version bumps"
  - version: 2.0.2
    changes:
      - "BREAKING: SYSTEM.md is now a fourth REQUIRED package file (.aiskill spec v2.2.0) — an invariant, versioned verification protocol every AI agent must follow before executing SKILL.md — packages built before this version lack it and no longer conform"
      - "New required manifest field system_protocol_version, declaring which SYSTEM.md version this package ships"
  - version: 2.0.1
    changes:
      - Bundled skill/LICENSE.txt (MIT) — conforms to the v2.1.0 .aiskill spec's optional bundled license file
  - version: 2.0.0
    changes:
      - "BREAKING: CARD.md is now a third REQUIRED package file, generated deterministically by build_card.py from manifest.yaml — packages built before this version no longer conform"
  - version: 1.3.2
    changes:
      - Current shipped state as recovered from the .aiskill archive -- specific changes since
        1.3.1 were not documented anywhere recoverable at retrofit time
  - version: 1.3.1
    changes:
      - Fix WCP_BUILTIN_THEMES exposed as window.WCP_BUILTIN_THEMES so the URL export IIFE can access it
      - Fix loadSeasonalThemes .catch no longer re-firing the callback if render threw
      - Add 4 seasonal .wcpt files bundled in assets/collections/ -- no longer sourced externally
      - Update Step 11 to reference bundled files with exact IDs and filenames
  - version: 1.3.0
    changes:
      - Add theme collections dropdown (My Themes, seasonal, user-imported)
      - Add URL export panel with shareable theme query string
      - Add URL export info modal
      - Add AI Skill PKG tab
  - version: 1.0.0-1.2.0
    changes:
      - Initial releases -- individual version changes not preserved, collapsed range as
        recorded in the shipped package's own README at retrofit time
